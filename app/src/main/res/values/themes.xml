<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RVTraining" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light and dark themes here. -->
        <item name="colorPrimary">@color/black</item>  <!-- Toolbar and primary elements color -->
        <item name="colorOnPrimary">@color/black</item>  <!-- Text color on primary color -->
        <item name="colorPrimaryVariant">@color/black</item>  <!-- Darker shade for status bar -->
        <item name="colorSecondary">@color/color22</item>  <!-- Used for secondary elements (buttons) -->
        <item name="colorOnSecondary">@color/white</item>  <!-- Text color on secondary color -->
        <item name="android:windowBackground">@color/black</item>  <!-- Background color for the app -->
        <item name="android:textColor">@color/white</item>  <!-- Default text color -->
    </style>

    <style name="Theme.RVTraining" parent="Base.Theme.RVTraining" />
</resources>
